def foobar : int
    @a : int
    @b : int

    return a + b

class Pair:
    x : int
    y : int

    def init : void
        @a : int
        @b : int

        return

    def reset : int
        x = 0
        y = 0
        z = reset()
        return x + y
