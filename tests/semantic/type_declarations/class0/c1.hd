class Player:
    x : int

class Hash<T, K>:
    key   : T
    value : K

def sum : int
    @a : int
    @b : int

    return a + b

def sum : int
    @a : int
    @b : int
    @c : int

    return a + b + c

def sum<T> : T
    @a : T
    @b : T

    return a + b

def main : void
    i = 0
    a = i
    b = a
    p = Hash<int, int>
    a = sum(2, 3)
    b = sum(2, 3, 4)
    c = sum<float>(2.0, 3.0)
    
