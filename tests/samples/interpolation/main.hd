import std.io
import std.string

def main : void
    n = 2
    m = 3
    msg = 'n + m = {n + m}'
    println(msg)

    for i = 0 - 10; i < 10; ++i:
        println('Hello from $i and from {i * 2}')
        println('Hello from \$i and from \{i * 2\}')
