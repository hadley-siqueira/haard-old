import std.io

def fib : int
    @n : int

    if n == 0 or n == 1:
        return 1
    else:
        return fib(n - 1) + fib(n - 2)

def main : void
    n = read_int('Enter a number: ')
    f = fib(n)
    println(f)

    n = read_int('Enter another number: ')
    f = fib(n)
    println(f)
