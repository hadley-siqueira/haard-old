def swap<T> : void
    @a : T*
    @b : T*

    tmp = *a
    *a = *b
    *b = tmp

class Node<T>:
    data : T
    next : Node<T>*
    prev : Node<T>*

    def foo : void
        a = 0
        b = 2
        swap<T>(&a, &b)

def main : void
    n = Node<int>()
    n.data = 0

