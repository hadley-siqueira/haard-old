import std.io
import std.cstring

def main : void
    var buffer : char[255]
    var inv : char[255]
    var b2 : int[10]

    v = buffer[0]
    v = buffer[7]
    buffer[0] = 'a'
    buffer[1] = '7'
    buffer[2] = '\0'
    println(buffer[0])
    println(buffer[1])

    println(&buffer[0])
    println(buffer)

    strcpy(buffer, "Hello, world!")
    strcpy(inv, buffer)

    println(buffer)
    strrev(inv)
    println(inv)
    
