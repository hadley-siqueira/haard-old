#include <stdint.h>
#include <iostream>
#include <string>
#include <sstream>
#include <vector>
#include <map>

void main() {
    auto p = ;
    p = ;
    p = ;
    auto v = set_obj(, "hello");
    ()[0] = 1;
    v = (1 + 2) * 3;
    v = a;
    (a, b) = (2, 3);
}

