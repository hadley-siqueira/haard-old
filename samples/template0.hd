def soma<T> : T
    @a : T
    @b : T

    return a + b

def soma : int
    @a : int
    @b : int

    return a + b

def soma<T, K> : K
    @a : T
    @b : K

    return b

def soma<T> : T
    @a : T

    return a + 1

def make_pair<A, B> : int
    @a : A
    @b : B

    return 0

def foo : int
    @a : int
    @b : int

    return a + b

def main : void
    a = foo(2, 3)
    b = soma<int>(2, 3)

