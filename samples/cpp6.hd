class Node:
    value : int
    next  : Node*
    prev  : Node*

class List:
    head : Node
    tail : Node

    def append : void
        @value : int

        node = new Node()
        node.value = value
        #node.next = tail # need to type check this
        node.next = &tail
        node.prev = tail.prev
